//! Border Run game mode: adjacency graph, pathfinding, random pair
//! generation, and the per-game state machine. All pure (no I/O).

pub mod game;
pub mod generator;
pub mod graph;
pub mod pathfinding;
